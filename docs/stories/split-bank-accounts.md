A banking connection can return mutliple different accounts (with different ibans but I currently only see the account_uid in the returned data, is this possible to include from the openbank api? Perhaps search the openbank docs first). Then, in the top left of the screen, add a select dropdown where you can select the different bank accounts. Whenever an account is selected, only the transactions from that account should be shown.