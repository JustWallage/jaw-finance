It is a bit too strict, often doesn't assign any tags at all while it could've suggested new ones.

Add a few examples of correct tagging scenario's (few shot), including the existing/rejected tags, and it must include some examples where it just assigns existing tags, and some where it assigns both existing and new tags.

